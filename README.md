# Pathawk

Мета-репозиторий: **код** в каталогах `server/`, `web/`, `desktop/`.

**Документация:** [docs/README.md](docs/README.md) — PRD, инженерное ТЗ, обзор репозиториев.

**Быстрый старт UI:** `cd desktop && npm install && npm run dev` (поднимет `web` на :5173 и Electron).
